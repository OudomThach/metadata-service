# Metadata Service configuration
# Copy to .env and adjust.

# Postgres (compose service metadata-postgres)
METADATA_DB_PASSWORD=metadata
METADATA_DB_PORT=5433

# API port exposed by the metadata-service container
METADATA_PORT=8095

# First admin user — seeded on first boot (needed for portal login).
METADATA_ADMIN_USERNAME=admin
METADATA_ADMIN_PASSWORD=change-me

# Optional machine keys for external API consumers (X-API-Key header).
# Portal users use their username+password instead. POST /records stays open.
METADATA_API_KEYS=

# Comma-separated CORS origins (e.g. https://portal.example.com).
# Leave empty when the portal is served same-origin.
METADATA_CORS_ORIGINS=
